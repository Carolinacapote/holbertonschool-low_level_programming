#include "holberton.h"
/**
 * mul - Function that multiplies 2 digits. Start point.
 *@a: digit to be multiplied
 *@b: second digit to be multiplied.
 * Return: result.
 */

int mul(int a, int b)
{
	int result = 0;

	result = (a * b);
	return (result);
}
